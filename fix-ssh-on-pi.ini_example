# Generate the password hash using `echo 'mypassword' | openssl passwd -6 -stdin`
#new_user='changeme'
#new_password_hash='changeme'
#public_key_file="/home/change_me/.ssh/id_ed25519_pi.pub"
#wifi_file="/home/change_me/.ssh/wpa_supplicant.conf"
first_boot="firstboot.sh"
# Choose the variant to download between full and lite versions
os_variant="full"
